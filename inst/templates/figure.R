#' ------------------------------------------------------------------------
#' Name: {{name}}
#' ID: {{id}}
#' Author: {{author}}
#' Created: {{creation_date}}
#' ------------------------------------------------------------------------


# Packages ----------------------------------------------------------------

library(ggplot2)

# Data --------------------------------------------------------------------

data <- iris

# Plot --------------------------------------------------------------------

plot <- ggplot(data, aes(x = Petal.Width, y = Petal.Length, color = Species)) +
  geom_point()

# Save --------------------------------------------------------------------

verbose_ggsave(
  name = "{{name}}",
  plot = plot,
  width = 6,
  height = 4,
  dpi = 300
)
